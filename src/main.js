require('./styles/styles.css');

import React from 'react';
import PasswordForm from './PasswordForm';

const component = <PasswordForm />;

React.render(component, document.getElementById('mount'));

